# Handoff — 12/08/2026

> Sessão longa. Tudo abaixo **já está em produção** (push na `main` → deploy Vercel), exceto o que está marcado como **A FAZER / plano**. Backend aplicado direto no Supabase (schema `"Teste ERP"`).

## 1. Entregue hoje (produção)

1. **Tabela de preço + validade (Orçamento/Vendas)** — removido o seletor de tabela; preço segue `clientes.id_tabela_preco` (padrão **Varejo** id 1). Validade do orçamento agora **automática** = emissão + `tabelas_preco.validade_dias` (nova coluna, default 30). `orcamento_salvar` reescrito (não zera tabela/validade ao editar).
2. **Distribuição — horário de lançamento** — `os_servicos`/`os_pecas` ganharam `criado_em` imutável (≠ `data_inicio`). `os_distribuicao_dados` devolve `criado_em` e ordena pela chegada; coluna "Lançado" na tela.
3. **Fix menu RH** — não aparecia porque faltava o **módulo de permissão** `rh`. Criado em **`modulos` E `modulos_sistema`** (id 21) e liberado ao grupo Administrador; itens do menu com `permKey:"rh"`. ⚠️ Ver §4 (duas tabelas de módulo).
4. **Colaborador = mestre da pessoa** — `usuarios.id_colaborador` (1:1) + `rh_colaborador.id_centro_custo`. Folha (`fn_folha_gerar_contas_pagar`) grava **empresa** (já vinha) e **centro de custo** (novo: colaborador → depto → default) no contas a pagar. `erp_colaborador_gerar_acesso` cria/vincula login herdando dados. Tela Colaboradores: Centro de custo + Empresa obrigatória + bloco "Acesso ao sistema".
5. **Vários orçamentos → mesma OS** — `orcamentos_venda.id_os`. OS: botão "Novo orçamento" (nav com contexto) + painel "Orçamentos desta OS" (`erp_os_orcamentos`). Orçamento com selo "OS <nº>".
6. **Remessa / Retorno (módulo novo)** — `rem_remessa`/`rem_item`/`rem_retorno`; RPCs salvar/obter/registrar_retorno/listar/dominios/**encerrar**. Status auto PENDENTE/PARCIAL/RETORNADA + ENCERRADA (baixa manual). Tela `Remessas.jsx` (Estoque → Remessas / Retornos).
7. **Guard de permissão nas RPCs novas** — `erp_exigir_permissao` em remessa (ESTOQUE), colaborador (RH), gerar acesso (USUARIOS). Front passa `_ator`.

## 2. Estado da SEGURANÇA (autorizações)

- **Autorização remota (sino)** — sistema completo (`autorizacoes` + solicitar/consultar/decidir/pendentes/autenticar_aprovador). Ligado em: desconto (`venda_lancar_item`, `orcamento_lancar_item`, `venda_solicitar_item`) e **faturamento** (`os_faturar`, `venda_faturar`). ✅ Funcionando.
- **Guard por usuário (server-side)** — `erp_exigir_permissao(ator, MODULO, acao)` → `erp_usuario_pode`. **Seguro por design:** retorna `true` p/ admin e p/ usuário **sem grupo** → ninguém é travado hoje; passa a valer quando grupos não-admin forem atribuídos.
- **Cobertura hoje:** só `orcamento_salvar`, `venda_salvar`, `venda_cancelar` + as 4 RPCs novas de hoje. **~85 RPCs de mutação ainda sem guard** (Furo 2).

## 3. A FAZER — plano do guard (revisar antes de aplicar)

Adicionar no topo de cada RPC de mutação: `PERFORM public.erp_exigir_permissao(nullif(p->>'_ator','')::int, '<MODULO>', '<incluir|editar|excluir>');` (e o front passar `_ator`). Módulos = `modulos_sistema.codigo` (MAIÚSCULO). Agrupamento sugerido das ~85:

| Módulo | RPCs (exemplos) |
|---|---|
| CLIENTES | cliente_salvar, erp_cliente_salvar, erp_cliente_contato_salvar/excluir |
| PRODUTOS | erp_produto_salvar, produto_salvar, produto_preco_salvar, produto_composicao_*, erp_preco_cliente_*, erp_preco_empresa_salvar, erp_grupo/subgrupo_produto_salvar, servico_salvar, grupo_servico_salvar, erp_area_servico_salvar, erp_grupo_markup_salvar |
| OS | os_salvar, os_servico_salvar, os_lancar_peca/producao, os_remover_peca, os_apontamento_*, os_cancelar, os_faturar, os_defeito_*, os_distribuir_*, os_prisma_* |
| ORCAMENTOS | erp_orcamento_salvar, (erp_)orcamento_aprovar, orcamento_converter_venda, orcamento_lancar/remover_item |
| VENDAS | venda_faturar, venda_lancar/remover_item, erp_devolucao_salvar/cancelar, encomenda_aprovar/cancelar |
| ESTOQUE / MOV_INTERNA | erp_baixar_estoque, erp_transferencia_salvar/cancelar, erp_recebimento_salvar/cancelar, erp_inventario_cancelar, tipos_saida_salvar, erp_gerar_curva_abc |
| COMPRAS | erp_pedido_compra_salvar, erp_cotacao_salvar, erp_cotacao_resposta_salvar, erp_cotacao_gerar_pedidos |
| FINANCEIRO_CR/CP, CAIXA | erp_titulo_salvar, erp_baixar_titulo, erp_aprovar_titulo, erp_estornar_baixa, erp_cheque_salvar, erp_conta_financeira_salvar, erp_centro_custo_salvar, erp_plano_conta_salvar, erp_forma_pagamento_salvar |
| RH | erp_dependente_salvar/remover, erp_empresa_encargo_salvar |
| FISCAL | erp_empresa_fiscal_salvar, erp_fiscal_empresa_salvar |
| CADASTROS/CONFIG | erp_config_salvar, erp_cobranca_config_salvar, erp_departamento_salvar, erp_unidade_salvar, transportadora_salvar, fornecedor_salvar, veiculo_salvar, grupo_tributario_salvar |
| USUARIOS | erp_usuario_salvar |

> **Não é ERP:** `assistencia_excluir_usuario`, `frt_gerar_codigo_cotacao` — pular.
> **Cuidado:** aplicar em lote é seguro (não trava ninguém hoje), mas cada `CREATE OR REPLACE` recria a função — recomendo aplicar por módulo, buscando a definição exata antes (evitar erro de transcrição em RPC crítica). Há **overloads** (os_salvar, servico_salvar, os_servico_salvar aparecem 2-3×) — checar assinatura p/ não gerar HTTP 300.

**Proteção de verdade = Camada 2 (JWT+RLS):** o `_ator` vem do front e pode ser forjado/omitido (`_ator` nulo = guard vira no-op). O definitivo é o servidor derivar o usuário do **JWT** (`auth.uid()`) e apertar RLS. Planos: `SEGURANCA_FURO2_JWT.md` / `Instrucoes\SEGURANCA_APPS_PLANO.md`.

**Operacional (Leo):** atribuir os ~50 usuários aos grupos — é o que "liga" todo o guard.

## 4. Armadilha confirmada — DUAS tabelas de módulo

`modulos` (chave minúscula: os, vendas, rh…) e `modulos_sistema` (codigo MAIÚSCULO). **Ids paralelos, semânticas diferentes.** `login_erp` monta `usuario.permissoes` via **`modulos`** (chave/visualizar); mas o FK de `grupos_permissoes.id_modulo` aponta pra **`modulos_sistema`**; e `erp_usuario_pode` casa por **`modulos_sistema.codigo`** (MAIÚSCULO). **Módulo novo = inserir a MESMA id nas duas tabelas.** No front, item de menu `ok:true` sem `permKey` usa a própria `key` como módulo → se não existir `modulos.chave` igual, o item some **até pra admin** (foi o bug do menu RH).

## 5. Backlog funcional

- Item 5 incremental: vendas/solicitação/peças derivarem dados da pessoa do colaborador (vínculo já existe).
- Remessa: "converter em devolução/garantia" (Encerrar manual já existe).
- Fiscal: motor de cálculo (monofásico, ST, DIFAL, apuração/SPED).
- RH: integrador eSocial XML, ponto/jornada, PLR, SST, migração dos ~147 do Firebird.
- Compras: Cotação (fluxo), tela Pedido persistente, fix stockout, lead time (SQL pronta), dívida de layout.
- OS: Fase A1 do front.
- Timestamps de mudança de status (OS/serviço) — proposto, adiado.

## 6. Commits da sessão
tabela preço/validade+fix RH · distribuição criado_em · colaborador mestre · orçamentos↔OS · remessa/retorno · guard RPCs novas · remessa Encerrar.
